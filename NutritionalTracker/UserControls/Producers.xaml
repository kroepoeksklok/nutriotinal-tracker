<UserControl x:Class="NutritionalTracker.UserControls.Producers"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:vm="clr-namespace:NutritionalTracker.ViewModels"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             DataContext="{x:Static vm:ViewModelLocator.ProducersViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel>
        <StackPanel Orientation="Vertical" DockPanel.Dock="Top">
            <StackPanel Orientation="Horizontal">
                <Label Width="100">Name:</Label>
                <TextBox Width="250"></TextBox>
            </StackPanel>
            <Button HorizontalContentAlignment="Left" HorizontalAlignment="Left" Padding="10 2 10 2" Command="{Binding AddProducer}">Add producer</Button>
        </StackPanel>
        <DataGrid Name="ProducersGrid" ItemsSource="{Binding ProducersView}" CanUserAddRows="False" CanUserDeleteRows="False">

        </DataGrid>
    </DockPanel>
</UserControl>
